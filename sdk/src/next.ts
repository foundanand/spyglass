// @spyglass/sdk/next — Next.js helper entry. The real <SpyglassProvider> that
// wires app-router pageviews lands in p1-sdk-next-provider (will become .tsx).
// Kept as a separate entry so the core bundle never pulls in React.

export { VERSION } from "./index.js";
